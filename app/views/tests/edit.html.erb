
<%= form_for [@teacher, @test] do |f| %>
	<!-- there are several submit buttons in this form which go to different actions in controlle --> 
	<!-- tests will be edited, link comes from the teachers show site -->
	<!-- form also creates question sets in the test controller --> 
	<%= render partial: "name_edit", locals: {f: f} %> 
	<%= render partial: "assign", locals: {f: f} %> 
	<%= render partial: "test_question_edit", locals: {f: f} %> 
<% end %>