<h1>Students#show</h1>
<p>Find me in app/views/students/show.html.erb</p>

<p>Student's name: <%= @student.name ? @student.name : "No name specified" %><p> 
<p>Student's group: <%= @student.group ? @student.group.name : "No group specified" %><p>
<p>Student's year: <%= @student.year ? @student.year : "No year specified" %> </p>

<% unless @student.group.tests.blank? %> 
	<p>Student's tests: </p>
	<% @student.group.tests.each do |test| %> 

		<% if @student.student_tests.grade %>
			<p>Your grade from the first test is: <%= @student.student_tests.first.grade %></p>
		<% else %>
			<p> Test id is: <%= test.id %> </p>
			<p>Take the test: <%= link_to "Link to student's test", take_exam_student_test_path(@student, test) %></p>
		<% end %>
		
	<% end %>
<% else %> 	
	<p> Currently there are no new tests </p>
<% end %>

<p><%= link_to "logout", controller: "access", action: "logout" %></p>




